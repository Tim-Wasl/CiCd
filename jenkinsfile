pipeline {
  agent any

  tools {
    jdk 'openjdk-11'
    maven 'Maven 3.8.1'
  }

  stages {
    stage('Build') {
      steps {
        bat 'mvn clean compile'
      }
    }
    stage('Quality Check') {
      steps {
        bat 'mvn verify'
      }
    }
  }
}