pipeline {
  agent any

  tools {
    jdk 'openjdk-11'
  }

  stages {
    stage('Build') {
      steps {
        sh 'mvn clean compile'
      }
    }
    stage('Quality Check') {
      steps {
        sh 'mvn verify'
      }
    }
  }
}