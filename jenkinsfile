pipeline {
  agent any

  tools {
    jdk 'openjdk-11'
  }

  stages {
    stage('Build') {
      steps {
        bat "./gradlew bootrun"
      }
    }
    stage('Quality Check') {
      steps {
        bat './gradlew build'
      }
    }
  }
}